<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- banner -->
<mapper namespace="com.duyi.onlinevideo.banner">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="bannerMap" type="Banner">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="img_url" property="imgUrl" jdbcType="VARCHAR" />
        <result column="flag" property="flag" jdbcType="VARCHAR" />
        <result column="type" property="type" jdbcType="INTEGER" />
        <result column="target_url" property="targetUrl" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        b.id, b.img_url, b.flag, b.type, b.target_url, b.create_time
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findBannerAll" resultMap="bannerMap">
        SELECT
        <include refid="allColumns" />
        FROM banner b
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findBannerByCondition" resultMap="bannerMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM banner b WHERE 1 = 1
        <if test="imgUrl != null and imgUrl != ''">
            AND b.img_url LIKE CONCAT('%', #{imgUrl}, '%')
        </if>
        <if test="flag != null">
            AND b.flag = #{flag}
        </if>
        <if test="type != null">
            AND b.type = #{type}
        </if>
        <if test="targetUrl != null and targetUrl != ''">
            AND b.target_url LIKE CONCAT('%', #{targetUrl}, '%')
        </if>
        <if test="createTime != null">
            AND b.create_time = #{createTime}
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findBannerByIds" resultMap="bannerMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM banner b WHERE b.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertBanner" parameterType="map">
        INSERT INTO banner (
            id, img_url, flag, type, target_url, create_time
        ) VALUES (
            #{id},
            #{imgUrl},
            #{flag},
            #{type},
            #{targetUrl},
            #{createTime}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateBanner" parameterType="map">
        UPDATE banner
        <set>
            <if test="imgUrl != null">
                img_url = #{imgUrl},
            </if>
            <if test="flag != null">
                flag = #{flag},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="targetUrl != null">
                target_url = #{targetUrl},
            </if>
            <if test="createTime != null">
                create_time = #{createTime}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 批量修改数据 -->
    <update id="updateBanners" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE banner
            <set>
                <if test="item.imgUrl != null">
                    img_url = #{item.imgUrl},
                </if>
                <if test="item.flag != null">
                    flag = #{item.flag},
                </if>
                <if test="item.type != null">
                    type = #{item.type},
                </if>
                <if test="item.targetUrl != null">
                    target_url = #{item.targetUrl},
                </if>
                <if test="item.createTime != null">
                    create_time = #{item.createTime}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteBannerById" parameterType="int">
        DELETE FROM banner WHERE id = #{id}
    </delete>
    
    <!-- 根据主键批量删除数据 -->
    <delete id="deleteBannerByIds" parameterType="list">
        DELETE FROM banner WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>