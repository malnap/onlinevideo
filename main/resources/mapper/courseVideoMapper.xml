<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- course_video -->
<mapper namespace="com.duyi.onlinevideo.courseVideo">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="courseVideoMap" type="CourseVideo">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="free_view" property="freeView" jdbcType="VARCHAR" />
        <result column="topic_id" property="topicId" jdbcType="INTEGER" />
        <result column="flag" property="flag" jdbcType="VARCHAR" />
        <result column="video_url1" property="videoUrl1" jdbcType="VARCHAR" />
        <result column="video_url2" property="videoUrl2" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        cv.id, cv.name, cv.free_view, cv.topic_id, cv.flag, cv.video_url1, cv.video_url2, cv.create_time
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findCourseVideoAll" resultMap="courseVideoMap">
        SELECT
        <include refid="allColumns" />
        FROM course_video cv
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findCourseVideoByCondition" resultMap="courseVideoMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM course_video cv WHERE 1 = 1
        <if test="name != null and name != ''">
            AND cv.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="freeView != null">
            AND cv.free_view = #{freeView}
        </if>
        <if test="topicId != null">
            AND cv.topic_id = #{topicId}
        </if>
        <if test="flag != null">
            AND cv.flag = #{flag}
        </if>
        <if test="videoUrl1 != null and videoUrl1 != ''">
            AND cv.video_url1 LIKE CONCAT('%', #{videoUrl1}, '%')
        </if>
        <if test="videoUrl2 != null and videoUrl2 != ''">
            AND cv.video_url2 LIKE CONCAT('%', #{videoUrl2}, '%')
        </if>
        <if test="createTime != null">
            AND cv.create_time = #{createTime}
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findCourseVideoByIds" resultMap="courseVideoMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM course_video cv WHERE cv.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertCourseVideo" parameterType="map">
        INSERT INTO course_video (
            id, name, free_view, topic_id, flag, video_url1, video_url2, create_time
        ) VALUES (
            #{id},
            #{name},
            #{freeView},
            #{topicId},
            #{flag},
            #{videoUrl1},
            #{videoUrl2},
            #{createTime}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateCourseVideo" parameterType="map">
        UPDATE course_video
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="freeView != null">
                free_view = #{freeView},
            </if>
            <if test="topicId != null">
                topic_id = #{topicId},
            </if>
            <if test="flag != null">
                flag = #{flag},
            </if>
            <if test="videoUrl1 != null">
                video_url1 = #{videoUrl1},
            </if>
            <if test="videoUrl2 != null">
                video_url2 = #{videoUrl2},
            </if>
            <if test="createTime != null">
                create_time = #{createTime}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 批量修改数据 -->
    <update id="updateCourseVideos" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE course_video
            <set>
                <if test="item.name != null">
                    name = #{item.name},
                </if>
                <if test="item.freeView != null">
                    free_view = #{item.freeView},
                </if>
                <if test="item.topicId != null">
                    topic_id = #{item.topicId},
                </if>
                <if test="item.flag != null">
                    flag = #{item.flag},
                </if>
                <if test="item.videoUrl1 != null">
                    video_url1 = #{item.videoUrl1},
                </if>
                <if test="item.videoUrl2 != null">
                    video_url2 = #{item.videoUrl2},
                </if>
                <if test="item.createTime != null">
                    create_time = #{item.createTime}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteCourseVideoById" parameterType="int">
        DELETE FROM course_video WHERE id = #{id}
    </delete>
    
    <!-- 根据主键批量删除数据 -->
    <delete id="deleteCourseVideoByIds" parameterType="list">
        DELETE FROM course_video WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>